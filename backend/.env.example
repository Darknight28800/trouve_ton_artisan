DB_HOST=localhost
DB_PORT=3306
DB_NAME=trouve_ton_artisan
DB_USER=root
DB_PASSWORD=
DB_DIALECT=mysql
PORT=5001

# Origine autorisée à appeler l'API (CORS) — l'URL du frontend
FRONTEND_URL=http://localhost:5173

# Utilisé pour signer les tokens JWT (auth admin) — obligatoire, générer une valeur aléatoire longue
JWT_SECRET=

# Identifiants SMTP pour l'envoi des emails de contact (ex: Gmail + mot de passe d'application)
MAIL_USER=
MAIL_PASS=
